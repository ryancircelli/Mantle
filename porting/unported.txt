# Mantle 1.21.1 porting frontier — main source set.
#
# Every path below is a .java file that has not been ported to 1.21.1 yet and is
# therefore excluded from sourceSets.main.java. Porting a file means deleting its
# line from this ledger, not adding one. checkPortingFrontier (wired into check)
# enforces that: it fails if a listed path no longer exists, and it fails if the
# ledger has gained an entry relative to the committed version at HEAD~.
#
# This file is deleted in the final PR of the port, when the frontier reaches zero.
# Sorted, one repo-relative path per line. Blank lines and # comments are ignored.

src/main/java/slimeknights/mantle/util/CombatHelper.java
